# Variables de entorno para SIDI Backend

# Database - SUPABASE (reemplaza con tus credenciales)
# Obtén estos valores de: Supabase → Settings → Database
DB_HOST=db.xxx.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=tu_password_de_supabase_aqui

# Para PostgreSQL local (descomenta si usas PostgreSQL local)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=sidi_db
# DB_USER=postgres
# DB_PASSWORD=tu_password_aqui

# Application
APP_NAME=SIDI API
APP_VERSION=1.0.0
DEBUG=True

# Security (cambiar en producción)
SECRET_KEY=tu_secret_key_super_segura_aqui_cambiar_en_produccion
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS (ajustar según necesidad)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000,https://hakaton-peach-sigma.vercel.app

# Machine Learning
ML_MODEL_PATH=data/models/svm_model.pkl
SCALER_PATH=data/models/scaler.pkl
